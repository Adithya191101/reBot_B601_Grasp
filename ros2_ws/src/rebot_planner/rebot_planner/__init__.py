"""reBot B601-DM Pinocchio planner (MoveIt replacement).

Keep this module import-light: importing :mod:`rebot_planner` (or
``rebot_planner.core``) must not pull in rclpy. The node module
(``planner_node``) is imported only by its console-script entry point.
"""
